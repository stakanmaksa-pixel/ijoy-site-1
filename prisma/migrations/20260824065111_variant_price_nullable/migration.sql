-- Цена модификации товара становится необязательной: для комбинаций
-- память/цвет/тип SIM, которых нет ни в одной строке прайса (но которые
-- показываются на сайте, чтобы покупатель мог уточнить цену у менеджера,
-- а не думал, что модификации не существует), price = NULL.
ALTER TABLE "ProductVariant" ALTER COLUMN "price" DROP NOT NULL;

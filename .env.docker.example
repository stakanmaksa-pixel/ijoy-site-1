# Для docker-compose: скопируйте в .env.docker и заполните,
# затем запускайте: docker compose --env-file .env.docker up -d --build

POSTGRES_USER=ijoy
POSTGRES_PASSWORD=CHANGE_ME
POSTGRES_DB=ijoy

BOT_API_TOKEN=CHANGE_ME
ADMIN_SESSION_SECRET=CHANGE_ME
